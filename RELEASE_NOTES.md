# v1.0.0

发布日期：2026-08-14

概述：
- 本次为项目的首次公开发布（v1.0.0）。包含项目的完整工程、`resolve_importer.py` 脚本和 macOS 应用构建相关文件。

包含内容：
- 完整工程源码与 Xcode 项目
- 用于解析 `.fcpxml` / `.fcpxmld` 的 `resolve_importer.py`
- 应用图标与打包脚本（`build_app.command`）
- 文档：`README.md`、`CHANGELOG.md`、`CONTRIBUTING.md`、`SECURITY.md`

修订说明：
- 将仓库中的 `README.md` 顶部版本对齐为 `v1.0.0`（此前文档存在 `v1.1` 标注，现已修改）。
- 为保持发布历史一致性，文档中更高的次版本标记已降级为 `v0.x`，以便与当前发布版本 `v1.0.0` 对应。

注意：
- 仓库不包含任何明文私钥或 API 密钥；若在历史中发现敏感信息，请立即与维护者联系并在相关服务中轮换密钥。
